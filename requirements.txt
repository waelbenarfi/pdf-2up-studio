Flask>=3.0
PyMuPDF>=1.24
Pillow>=10.0
numpy>=1.24
# Sert uniquement quand DATABASE_URL est definie (Suivi des lives sur
# PostgreSQL). En local, sans cette variable, SQLite suffit.
psycopg2-binary>=2.9
